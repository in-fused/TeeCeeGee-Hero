# RUNBOOK

## Important
This system **never uses mock or fake data**.

If a connector is not configured, endpoints return:
- status: NO_DATA_AVAILABLE
- reason: explicit

## Environment Variables
All external connectors are disabled unless configured.
